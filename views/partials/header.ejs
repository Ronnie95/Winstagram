<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="/scripts/app.js" defer></script>
    <link rel="stylesheet" href="/styles/main.css">
    <title>
        <% if(typeof name !== 'undefined') { %>
            Winstagram: <%= name %>
        <% } else { %>
            Winstagram: Undefined
        <% } %>
    </title>
</head>
<body>
    <nav>
        <a href="/">Home</a>
        <a href="/users">All Users</a>
        <a href="/users/signup">Create a New Account</a>
        <% if (typeof userLoggedIn !== "undefined") { %>
            <a href="/users/<%=userLoggedIn.id%>">My Account: <%=userLoggedIn.username%></a>
            <a href="/users/logout">Log Out</a>
        <% } else { %>
        <a href="/users/login">Log in to Your Account</a>
        <% } %>
    </nav>